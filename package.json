{
  "name": "successbridge",
  "version": "1.0.0",
  "description": "SuccessBridge Learning Platform",
  "scripts": {
    "build": "cd Server && npm install && npm run build",
    "start": "cd Server && npm start",
    "dev": "cd Server && npm run dev"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  }
}